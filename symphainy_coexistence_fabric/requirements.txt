# Symphainy Platform Dependencies
# Generated from pyproject.toml
#
# Optional dependencies:
# - For AS2 decryption support: Install pyas2lib>=1.4.4
#   Or use: poetry install --extras as2
#   Or install from: requirements-as2.txt

# Core
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic[email]>=2.5.0  # Email validation support

# Infrastructure
redis>=5.0.0
python-arango>=7.6.0
python-consul>=1.1.0

# Communication
websockets>=11.0.0
httpx>=0.24.0
meilisearch>=0.38.0
supabase>=2.0.0
python-multipart>=0.0.6

# Cryptography (used for JWT, AS2 fallback, etc.)
cryptography>=41.0.0

# Google Cloud Storage (REQUIRED for file storage - foundational capability)
google-cloud-storage>=2.10.0
google-auth>=2.23.0

# Visualization (REQUIRED for visual generation adapter - foundational capability)
plotly>=5.0.0
numpy>=1.24.0  # Required by plotly

# Data Processing (REQUIRED for parsing and deterministic compute)
duckdb>=0.9.0  # DuckDB for deterministic compute and data processing
PyYAML>=6.0.0  # YAML parsing for data model processing (JSON Schema, YAML schemas)

# EDA and Excel (REQUIRED per platform contract §9.4)
pandas>=2.0.0   # EDA analysis agent, Excel parsing (preferred path)
openpyxl>=3.1.0  # Excel parsing (pandas read_excel engine)

# OpenTelemetry (REQUIRED per platform contract - OTLP tracing/metrics/logs)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp-proto-grpc>=1.20.0
opentelemetry-exporter-otlp-proto-http>=1.20.0
opentelemetry-instrumentation-logging>=0.41b0
opentelemetry-instrumentation-fastapi>=0.41b0
